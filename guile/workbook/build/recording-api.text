Part of the release process: record the API.

To do this, in top-level builddir, do:

  $mscripts/update-guile-api.alist

This checks out $top_srcdir/doc/guile-api.alist,
     runs $mscripts/scan-api to update it,
     and finally commits the new file.
