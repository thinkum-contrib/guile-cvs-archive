# Automatically added by dh_installdocs
if [ "$1" = "configure" ]; then
	if [ -d /usr/doc -a ! -e /usr/doc/goops -a -d /usr/share/doc/goops ]; then
		ln -sf ../share/doc/goops /usr/doc/goops
	fi
fi
# End automatically added section
